import ROUTES from "../../routes/ROUTES";

const alwaysLinks = [
    { to: ROUTES.HOME, children: "Home" },
    { to: ROUTES.LOGIN, children: "login" },
    { to: ROUTES.LOGIN, children: "Register" },
];
export { alwaysLinks};